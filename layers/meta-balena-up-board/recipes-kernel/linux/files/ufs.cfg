# UFS host controller support
# Needed for boards whose onboard storage is UFS (e.g. Wildcat Lake based
# UP Nexus WCL) rather than eMMC/SATA/NVMe - without this the onboard
# storage device never shows up as a block device.
CONFIG_SCSI_UFSHCD=y
CONFIG_SCSI_UFSHCD_PCI=y
CONFIG_SCSI_UFS_HWMON=y
